
#[derive(Copy, Clone)]
pub struct State {}

impl State {
    /// Create new State
    /// 
    /// stream is tcp stream
    /// 
    /// # Panic
    /// 
    pub fn new() -> State {
        State {}
    }
}